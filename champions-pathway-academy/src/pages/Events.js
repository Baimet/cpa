// src/components/Events.js
/*
import React from "react";
import "./Events.css";

function Events() {
  const events = [
    {
      title: "Launch of ECTA",
      description:
        "The official launch of the academy, marking the beginning of a new journey in nurturing talent and empowering youth through sports.",
      date: "11th April 2026",
      time: "8:00 AM",
      location: "Yikitise Primary School, Kitise Location",
      image: "/images/launch.jpeg",
      invite: "All are invited!",
    },
  ];

  return (
    <div className="events-container">
      <h1>Upcoming Events</h1>
      <p className="events-intro">
        Stay updated with our latest events and activities at Empower Champions
        Talent Academy.
      </p>
      <div className="events-grid">
        {events.map((event, index) => (
          <div key={index} className="event-card">
            <img src={event.image} alt={event.title} className="event-image" />
            <h2>{event.title}</h2>
            <p className="event-description">{event.description}</p>
            <p>
              <strong>Date:</strong> {event.date}
            </p>
            <p>
              <strong>Time:</strong> {event.time}
            </p>
            <p>
              <strong>Location:</strong> {event.location}
            </p>
            <p className="event-invite">{event.invite}</p>
          </div>
        ))}
      </div>
    </div>
  );
}

export default Events;
*/
